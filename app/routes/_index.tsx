import { Header, HeroSection, StackSection } from '~/layouts/landing'

const Home = () => {
  return (
    <div className="overflow-x-clip pb-10">
      <Header />
      <HeroSection />
      <StackSection />
    </div>
  )
}

export default Home
